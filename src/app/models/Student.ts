export class Student {
    name: string;
    id: string;
    year: number;

    constructor(name: string, id: string, year: number) {
        this.name = name;
        this.id = id;
        this.year = year;
    }
}